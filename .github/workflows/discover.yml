name: 🔍 Discover Site IDs (Run Once)

on:
  workflow_dispatch:

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - name: Install camply
        run: pip install camply

      - name: Find Bruce Peninsula Recreation Area
        run: camply recreation-areas --provider CanadaParks --search "Bruce Peninsula"
